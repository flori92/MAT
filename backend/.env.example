HOST=127.0.0.1
PORT=8787

# URL du pipeline OCR local (Python FastAPI + PaddleOCR + doctr + manga-ocr)
OCR_PIPELINE_URL=http://127.0.0.1:8788
OCR_PIPELINE_TIMEOUT_MS=120000

# Cache de traduction (24h par défaut)
TRANSLATION_CACHE_TTL_MS=86400000

# Optional Bearer token required by the extension.
BACKEND_AUTH_TOKEN=
